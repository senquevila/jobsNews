String.prototype.replaceAll = function(search, replacement) {
  var target = this;
  return target.replace(new RegExp(search, 'g'), replacement);
}; 

module.exports.clearSpace = function (valStr){
	valStr = valStr.replaceAll('\r\n', '');
	valStr = valStr.replaceAll('\t', '');

	return valStr;
};